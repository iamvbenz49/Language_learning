let sales: number = 123_4_5; // we can add _ to seperate digits
let course: string = "typescript";
let num = 10; //datatype will be assigned automatically if we didn't assign the type
let n; //if we create a new variable and didn't initialise it it willl assume it to be any datatype
let anyy:any;